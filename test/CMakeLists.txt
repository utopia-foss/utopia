# WARNING: Do NOT include the 'dependency' directory into this build tree.
#          It is intended for testing use only.

# Continue in the config tree to include the tests
add_subdirectory(core)
add_subdirectory(data_io)
