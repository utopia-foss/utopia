---
seed: 42
output_path: model_test_tmpfile.h5
num_steps: 3
write_every: 1
monitor_emit_interval: 1.5

# model configuration (no values expected here)
test: {}

# custom configuration
custom_cfg:
  foo: bar
  note: this is the custom configuration node

log_levels:
  core: debug
  model: debug
  data_io: debug
