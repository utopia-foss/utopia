# Config file for the test of the PseudoModel for setup of model classes
---
seed: 42
output_path: model_setup_test_tmpfile.h5
num_steps: 3
monitor_emit_interval: 5.

# How frequently to call write_data; passed along to submodels
write_every: 3

model1:
  foo: bar

model2:
  bar: foo
  write_every: 1

model3:
  spam: eggs

log_levels:
  core: debug
  model: debug
  data_io: debug
