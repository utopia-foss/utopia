# Configuration file used to test that the initialization of the model is correct

---

parameter_space:
  num_steps: 0

# Load the model config
  ContDisease: !model
    model_name: ContDisease  # imports the model config for the ContDisease model
    initial_state: empty
    stones: false
    infection_source: true
    infection_source_loc: south

    grid_size: [32,32]
