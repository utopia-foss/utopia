# Configuration file used to test that the initialization of the model is correct

---

parameter_space:
  num_steps: 0

# Load the model config
  ContDisease: !model
    model_name: ContDisease  # imports the model config for the CopyMe model
    initial_state: empty
    stones: true
    infection_herd: false
    stone_density: 0.8

    grid_size: [32,32]
