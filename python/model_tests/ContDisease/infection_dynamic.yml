# Configuration file used to test that the infection dynamic of the model is correct

---

parameter_space:
  num_steps: 3

# Load the model config
  ContDisease: !model
    model_name: ContDisease  # imports the model config for the ContDisease model
    cell_manager:
      grid:
        resolution: 3
    cell_params:
      initial_density: 0


    stones: false
    infection_source: true

    p_growth: 1
    p_infect: 1
    p_random_infect: 0
