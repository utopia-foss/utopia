# Config for testing that the model does the expected for a specific minimal scenario
#
# The testing scenario is a single 
#
# NOTE that this sets only the common values; the changes are set via passing
#      additional keys to the the ModelTest.create_run_load method
---
perform_sweep: false

parameter_space:
  # Do a number of steps
  num_steps: 2

  # Load the default model config
  PredatorPrey: !model
    model_name: PredatorPrey

    space:
      extent: [2, 1]

    cell_manager:
      grid:
        structure: square
        resolution: 1

      #cell_params:
    initial_state: fraction

    e_init_prey: 2.0
    e_init_pred: 2.0    

    predprey_prob: 0.0
    predprey_frac: 0.0

    cost_of_living_pred: 1
    cost_of_living_prey: 1
    cost_of_repro_pred: 2
    cost_of_repro_prey: 2

    delta_e_pred: 0
    delta_e_prey: 0      
