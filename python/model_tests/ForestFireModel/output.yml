# Configuration file used to test that the output of the model is correct
---
perform_sweep: true

parameter_space:
  num_steps: 3

  seed: !sweep
    default: 42
    range: [4]

  ForestFireModel: !model  
    model_name: ForestFireModel  # imports the model config for the ForestFireModel
