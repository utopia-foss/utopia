# Configuration file used to test advanced model behaviour

# TODO specify what this config file intends to test here
# TODO rename this file to match the intended test purpose
---
perform_sweep: true

parameter_space:
  num_steps: 3

  seed: !sweep
    default: 42
    values: [23, 42, 665, 667]

  # TODO change "dummy" to the name of your model instance
  dummy: !model  
    model_name: dummy  # imports the model config for the dummy model

    # NOTE Below, entries from the model config can be changed or added
    foo: bar
