# A test configuration that runs a parameter sweep over the seed
---
parameter_space:
  num_steps: 5
  seed: !sweep
    default: 1337
    values: [23, 42, 665, 667]
    
  dummy: !model
    model_name: dummy  
