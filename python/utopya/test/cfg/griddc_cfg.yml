---
parameter_space:
  num_steps: 1  # Just write one step.

# Load the model config
  ContDisease: !model
    model_name: ContDisease  
    grid_size: [5, 7]        # Assure that the grid is small.


