### Plots configuration for basic uni plots on CopyMe model
---
# Plot the distance map of density G to itself
distance_map:
  based_on: .basic_uni.distance_map
  model_name: CopyMe

  path_to_data: some_state

  transform_data:
    - mean:
        dim: [x, y]