# Test file for the GraphGroup and for graph plotting, using CopyMeGraph.
---
parameter_space:
  num_steps: 3

  CopyMeGraph: !model
    model_name: CopyMeGraph

    create_graph:
      num_vertices: 10
      mean_degree: 2