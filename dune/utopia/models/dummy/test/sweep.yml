# A test configuration for the dummy model
---
worker_manager:
  num_workers: 2

parameter_space: !pspace
  num_steps: 5
  
  # Perform a sweep over the RNG seed
  seed: !sweep
    default: 1337
    values: [23, 42, 665, 667]

  # Load the default configuration via the yaml tag
  dummy: !model
    model_name: dummy
