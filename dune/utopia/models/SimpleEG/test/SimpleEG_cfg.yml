# A test configuration for the SimpleEG model, carried out by the frontend
---
worker_manager:
  num_workers: 2

parameter_space: !pspace
  num_steps: 5
  SimpleEG:
    seed: !sweep
      default: 1337
      values: [23, 42, 665, 667]
