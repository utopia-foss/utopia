# Uses a hexagonal grid discretization
---
paths:
  model_note: hex_grid

parameter_space:
  num_steps: 500

  ContDisease:
    cell_manager:
      grid:
        structure: hexagonal
      neighborhood:
        mode: hexagonal
