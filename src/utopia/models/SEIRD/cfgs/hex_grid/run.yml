# A run configuration that configures a hexagonal grid
---
parameter_space:
  num_steps: 365

  SEIRD:
    cell_manager:
      grid:
        structure: hexagonal
      neighborhood:
        mode: hexagonal
