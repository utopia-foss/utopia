# Run configuration for a universe run of the ForestFire model
---
# Overwrite parameter space settings
parameter_space:
  num_steps: 1000
  write_every: 5

  ForestFire:
    cell_manager:
      grid:
        resolution: 128

    p_lightning: 1.0e-4
